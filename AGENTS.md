<!-- BREAKWAVE AUTO-AGENT V2 MANAGED START -->
# BreakWave Auto-Agent + Shadow CI V2

Read `.auto-agent/project_identity.json`, `.auto-agent/project.json`, the one authoritative active plan, and `.auto-agent/framework/CORE_RULES.md` before changing files.

BreakWave-specific safety and product rules outrank reusable framework examples. Rescue must never be blocked, delayed, paywalled, or made dependent on CI, billing, backend, onboarding, or entitlement state. Billing failure must never become recovery failure.

Verify the exact GitHub account, repository, origin, branch, HEAD, origin HEAD, and clean tree before mutation. Create a safety branch before production work. Use one focused commit per stage. Never tag automatically. Never force-push production.

Shadow CI must test every incremental tree and the final combined tree. Production promotion requires an exact promotion lock tied to the authoritative plan, payload hashes, final tree, evidence ZIP, workflow run, workflow job, and conclusion.

A network, API, timeout, or polling interruption is not a CI failure and must not cause a repair commit.

Visible code blocks in chat must not exceed 40 lines. Larger scripts belong in checksum-locked ZIP packages.
<!-- BREAKWAVE AUTO-AGENT V2 MANAGED END -->
